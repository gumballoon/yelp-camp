<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Yelp Camp</title>

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">

    <link rel="stylesheet" href="/stylesheets/app.css">
</head>
<body class="d-flex flex-column justify-content-between" id="body-home">
    <header class="d-flex justify-content-end mt-2 p-2 opacity-75">
        <div class="ms-auto">
            <% if (!currentUser) { %>
              <a class="text-white text-decoration-none mx-2 home-nav" href="/login">Login</a>
              <a class="text-white text-decoration-none mx-2 home-nav" href="/Register">Register</a>
            <% } else { %>
              <a class="text-white text-decoration-none mx-2">@<%= currentUser.username %></a>
              <a class="text-white text-decoration-none mx-2 home-nav" href="/logout">Logout</a>
            <% } %>
          </div>
    </header>

    <main class="cover-container p-0 m-0 text-center text-white opacity-75">
        <h1 class="display-3 mb-3 fw-normal">Yelp Camp</h1>
        <p class="lead my-3">Discover top-rated campsites across the country</p>
        <a href="/campgrounds" class="btn btn-outline-light mb-3 ">View Campgrounds</a>
    </main>

    <footer class="card text-start bg-dark rounded-0">
        <div class="card-footer text-light text-opacity-50">
          Yelp Camp &copy; 2025
        </div>
    </footer>
</body>
</html>